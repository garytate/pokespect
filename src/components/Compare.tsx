import React, { useState } from "react";

export default function Compare(props: any) {
	const [leftPokemon, setLeftPokemon] = useState();
	const [rightPokemon, setRightPokemon] = useState();

	return (
		<p>Compare Component</p>
	)
}
